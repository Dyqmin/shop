# microservices-orders-schema

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test microservices-orders-schema` to execute the unit tests via [Jest](https://jestjs.io).
