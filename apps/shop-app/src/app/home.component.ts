import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'shop-project-home',
  template: `home component`,
})
export class HomeComponent {
  title = 'shop-app';
}
